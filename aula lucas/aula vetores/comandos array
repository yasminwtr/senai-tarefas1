.push() -> adiciona um elemento no final
.pop() -> remove um elemento do final
.unshift() -> adiciona um elemento no começo
.shift() -> remove um elemento do começo
.concat() -> concatena dois vetores
.sort() -> ordena o vetor
.indexOf() -> retorna o índice da primeira vez que o elemento aparece
.lastIndexOf() -> retorna índice da última vez que o elemento aparece


.fill() -> preenche o vetor (ou parte dele) com um valor único
.filter() -> cria um vetor com os elementos que passaram no filtro (função)
.join() -> junta os elementos de um vetor em uma string e retorna ela
.map() -> aplica uma função em cada elemento do vetor e retorna um novo vetor
.slice() -> cria um novo vetor com parte do vetor original
.splice() -> altera um vetor, podendo adicionar e remover elementos (posição, qnts quer remover, adicionar)
