// indices = posicao dentro do array, por ex: 2 = 0, 44 = 1
let numeros = [2, 44, 7, 2, 8, 10, 3, 12]
let cores = ['amarelo', 'violeta', 'verde', 'branco', 'azul', 'preto', 'vermelho']
let nomes = ['ana', 'isabella', 'lígia', 'roberto']
let valores = [100, 120, 80, 50, 45]

valores.fill()
cores.join

//let mistura = nomes.concat(valores)

numeros.shift()
valores.unshift(120)

//let indice = nomes.indexOf('isabella')

for(i = 0; i < 4; i++){
    numeros.pop()
}

let vetor = numeros

// funcao splice
nomeExclui = 'isabella'
nomeAtualiza = 'bella'
indiceExclui = 0

for(i = 0; i < nomes.length-1; i++){
    if(nomes[i] == 'isabella'){
        indiceExclui = i
    }
}

nomes.splice(indiceExclui, 1, nomeAtualiza)

//console.log(vetor);
//console.log(indice);
//console.log(mistura);
//console.log(valores);
console.log(nomes);

// cod do haru
let numeros = [2, 44, 7, 2, 8, 10, 3, 12,]
let cores = ['amarelo', 'violeta', 'branco', 'azul', 'preto']
let nomes = ['rafel', 'isabela', 'ana', 'roberto', 'ligia']
let valores = [3, 41, 8, 22, 7, 4, 10, 9]

nomeExclui = "isabela"
indiceExclui = 0

for(i=0; i < nomes.lenght-1; i++){
if(nomes[i]== 'isabela'){
indiceExclui = i

}
}

console.log(nomes)

nomes.splice(1, 1)
let soma = Number(numeros[0]) + Number(numeros[6])
let soma = Number(numeros[numeros.lenght -1])


let soma = numeros.length
console.log(soma)

numeros.shift
valores.unshift(120)
let indice = nomes.indexOf('Ana')



numeros.push(80)

for (i=0; i < 4; i++){

numeros.pop()
}

let vetor = numeros
console.log(vetor);

console.log(vetor);
console.log(indice);
console.log(mistura);
console.log(valores);





